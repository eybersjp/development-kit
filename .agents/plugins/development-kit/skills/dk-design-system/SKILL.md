---
name: dk-design-system
description: Establishes, inspects, verifies, and amends the authoritative Development Kit frontend design system.
---

# DK Design System

## Overview

Antigravity-native workflow entry point for `/dk-design-system`. The authoritative workflow definition remains `commands/dk-design-system.md`.

## Process

1. Before taking workflow action, read `../../commands/dk-design-system.md` relative to this `SKILL.md`.
2. Treat that command document as the single authoritative workflow specification for routing, sub-modes, skills, gates, state, and output requirements.
3. Preserve and apply any sub-mode or user text supplied with `/dk-design-system`, including `create`, `reference`, `existing`, `inspect`, `verify`, and `amend`.
4. Do not duplicate or reinterpret the workflow in this adapter.
5. If the authoritative command document cannot be read, stop and report that the Development Kit installation is incomplete.
