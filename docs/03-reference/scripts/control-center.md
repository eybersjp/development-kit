# control-center.mjs

The `control-center.mjs` script launches the local Development Kit Control Center service.

## Purpose

Starts the local loopback web service that hosts the Control Center UI and Runtime API.

## Usage

```bash
node scripts/control-center.mjs
node scripts/control-center.mjs --no-browser
node scripts/control-center.mjs --port=3200
```
